/** @format */
import { ButtonMyLocation, MapView, ReactLogo, SearchBar } from '../components';

export const HomeScreen = () => {
	return (
		<div>
			<ButtonMyLocation />
			<MapView />
			<ReactLogo />
			<SearchBar />
		</div>
	);
};
